spring:
  application:
    name: Email


eureka:
  instance:
    prefer-ip-address: true
  client:
    fetchRegistry: true
    fetchWithEureka: true
    serviceUrl:
      defaultZone: "http://localhost:8075/eureka"

server:
  port: 8088

logging:
  level:
    com.Email: DEBUG

